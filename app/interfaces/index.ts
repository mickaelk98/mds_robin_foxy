import { LoginData, RegisterData, User, UserType } from "@/app/interfaces/user";
import { AuthState } from "@/app/interfaces/auth";

export type { LoginData, RegisterData, User, AuthState, UserType };
